export { GET, POST } from '@/auth';
// export const runtime = "edge"; // Optional: For Edge compatibility if your adapter and providers support it.
// Prisma adapter might not be fully Edge compatible without specific configurations (e.g. Data Proxy).
// For now, we'll stick to Node.js runtime.
